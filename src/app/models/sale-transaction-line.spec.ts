import { SaleTransactionLine } from './sale-transaction-line';

describe('SaleTransactionLine', () => {
  it('should create an instance', () => {
    expect(new SaleTransactionLine()).toBeTruthy();
  });
});
